name: emergo
description: Emergency Response Mobile Application
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  flutter_dotenv: ^5.0.2
  http: ^1.1.0
  google_maps_flutter: ^2.3.0
  geolocator: ^9.0.2
  shared_preferences: ^2.1.1
  intl: ^0.18.1
  permission_handler: ^11.0.1
  sensors_plus: ^4.0.2
  # flutter_local_notifications: ^16.3.2  # Temporarily disabled due to build issues
  geocoding: ^2.1.1
  url_launcher: ^6.2.1
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/